{"pair_id": "py-sem-groupby", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "group_by_key", "text": "def group_by_key(rows, key):\n    out = {}\n    for row in rows:\n        out.setdefault(row[key], []).append(row)\n    return out"}, "right": {"kind": "inline", "name": "bucket_rows", "text": "def bucket_rows(rows, field):\n    result = defaultdict(list)\n    for item in rows:\n        result[item[field]].append(item)\n    return dict(result)"}, "notes": "setdefault vs defaultdict. Identical output including plain-dict return type."}
{"pair_id": "py-sem-flatten", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "flatten", "text": "def flatten(nested):\n    out = []\n    for group in nested:\n        for item in group:\n            out.append(item)\n    return out"}, "right": {"kind": "inline", "name": "concat_all", "text": "def concat_all(nested):\n    return list(itertools.chain.from_iterable(nested))"}, "notes": "Nested loop vs itertools.chain. Same order, same contents."}
{"pair_id": "py-sem-maxby", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "highest_scorer", "text": "def highest_scorer(players):\n    best = None\n    for player in players:\n        if best is None or player.score > best.score:\n            best = player\n    return best"}, "right": {"kind": "inline", "name": "top_player", "text": "def top_player(players):\n    if not players:\n        return None\n    return max(players, key=lambda p: p.score)"}, "notes": "Manual scan vs max(key=). Both return None on empty and both keep the first of a tie."}
{"pair_id": "py-sem-titlecase", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "title_words", "text": "def title_words(text):\n    words = text.split()\n    return \" \".join(w[0].upper() + w[1:] for w in words if w)"}, "right": {"kind": "inline", "name": "capitalize_each", "text": "def capitalize_each(sentence):\n    result = []\n    for word in sentence.split():\n        if word:\n            result.append(word[0].upper() + word[1:])\n    return \" \".join(result)"}, "notes": "Generator expression vs accumulator loop."}
{"pair_id": "py-sem-count", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "count_values", "text": "def count_values(items):\n    counts = {}\n    for item in items:\n        counts[item] = counts.get(item, 0) + 1\n    return counts"}, "right": {"kind": "inline", "name": "tally", "text": "def tally(items):\n    return dict(Counter(items))"}, "notes": "Manual counting vs collections.Counter."}
{"pair_id": "py-sem-chunkgen", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "chunks", "text": "def chunks(seq, size):\n    for i in range(0, len(seq), size):\n        yield seq[i:i + size]"}, "right": {"kind": "inline", "name": "windows", "text": "def windows(seq, size):\n    start = 0\n    while start < len(seq):\n        yield seq[start:start + size]\n        start += size"}, "notes": "range-step vs while loop. Identical yields including the short final chunk."}
{"pair_id": "py-sem-anymatch", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "has_admin", "text": "def has_admin(users):\n    for user in users:\n        if user.role == \"admin\":\n            return True\n    return False"}, "right": {"kind": "inline", "name": "contains_admin", "text": "def contains_admin(users):\n    return any(u.role == \"admin\" for u in users)"}, "notes": "Early-return loop vs any(). Same short-circuit semantics."}
{"pair_id": "py-sem-safeget", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "deep_get", "text": "def deep_get(data, path, default=None):\n    current = data\n    for key in path.split(\".\"):\n        if not isinstance(current, dict) or key not in current:\n            return default\n        current = current[key]\n    return current"}, "right": {"kind": "inline", "name": "lookup_path", "text": "def lookup_path(obj, dotted, fallback=None):\n    node = obj\n    for part in dotted.split(\".\"):\n        try:\n            node = node[part]\n        except (KeyError, TypeError):\n            return fallback\n    return node"}, "notes": "isinstance guard vs try/except. Same result for dict inputs; both return the fallback on a missing or non-dict node."}
{"pair_id": "py-sem-normalize", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "normalize_phone", "text": "def normalize_phone(raw):\n    digits = \"\".join(c for c in raw if c.isdigit())\n    if len(digits) == 11 and digits.startswith(\"1\"):\n        digits = digits[1:]\n    return digits"}, "right": {"kind": "inline", "name": "clean_phone_number", "text": "def clean_phone_number(value):\n    digits = re.sub(r\"\\D\", \"\", value)\n    if digits.startswith(\"1\") and len(digits) == 11:\n        return digits[1:]\n    return digits"}, "notes": "Comprehension filter vs regex sub, conditions in swapped order."}
{"pair_id": "py-sem-retryloop", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "fetch_with_retry", "text": "def fetch_with_retry(url, attempts=3):\n    for attempt in range(attempts):\n        try:\n            return http_get(url)\n        except TimeoutError:\n            if attempt == attempts - 1:\n                raise\n    return None"}, "right": {"kind": "inline", "name": "get_resilient", "text": "def get_resilient(url, tries=3):\n    remaining = tries\n    while remaining > 0:\n        remaining -= 1\n        try:\n            return http_get(url)\n        except TimeoutError:\n            if remaining == 0:\n                raise\n    return None"}, "notes": "for/range vs while/counter, same retry count and same re-raise on exhaustion."}
{"pair_id": "py-sem-dedupeorder", "split": "holdout", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "unique_ordered", "text": "def unique_ordered(items):\n    seen = set()\n    out = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            out.append(item)\n    return out"}, "right": {"kind": "inline", "name": "distinct", "text": "def distinct(items):\n    return list(dict.fromkeys(items))"}, "notes": "Explicit seen-set vs dict.fromkeys. Both preserve first-occurrence order."}
{"pair_id": "py-sem-slug", "split": "holdout", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "slugify", "text": "def slugify(title):\n    lowered = title.lower().strip()\n    out = []\n    for ch in lowered:\n        out.append(ch if ch.isalnum() else \"-\")\n    return \"-\".join(filter(None, \"\".join(out).split(\"-\")))"}, "right": {"kind": "inline", "name": "to_slug", "text": "def to_slug(text):\n    return re.sub(r\"[^a-z0-9]+\", \"-\", text.lower().strip()).strip(\"-\")"}, "notes": "Character loop vs regex. Same collapsing of runs and same trimming."}
{"pair_id": "py-sem-partition", "split": "holdout", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "split_valid", "text": "def split_valid(records):\n    good, bad = [], []\n    for record in records:\n        if record.is_valid:\n            good.append(record)\n        else:\n            bad.append(record)\n    return good, bad"}, "right": {"kind": "inline", "name": "partition_records", "text": "def partition_records(records):\n    return (\n        [r for r in records if r.is_valid],\n        [r for r in records if not r.is_valid],\n    )"}, "notes": "Single pass vs two comprehensions. Same tuple of lists, same ordering."}
{"pair_id": "py-sem-sumdict", "split": "holdout", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "total_by_user", "text": "def total_by_user(payments):\n    totals = {}\n    for payment in payments:\n        totals[payment.user_id] = totals.get(payment.user_id, 0) + payment.amount\n    return totals"}, "right": {"kind": "inline", "name": "aggregate_amounts", "text": "def aggregate_amounts(payments):\n    totals = defaultdict(int)\n    for p in payments:\n        totals[p.user_id] += p.amount\n    return dict(totals)"}, "notes": "get-with-default vs defaultdict accumulation."}
{"pair_id": "py-ren-validate", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "validate_password", "text": "def validate_password(password):\n    errors = []\n    if len(password) < 8:\n        errors.append(\"too_short\")\n    if not any(c.isdigit() for c in password):\n        errors.append(\"no_digit\")\n    return errors"}, "right": {"kind": "inline", "name": "check_password", "text": "def check_password(candidate):\n    problems = []\n    if len(candidate) < 8:\n        problems.append(\"too_short\")\n    if not any(ch.isdigit() for ch in candidate):\n        problems.append(\"no_digit\")\n    return problems"}, "notes": "Pure rename."}
{"pair_id": "py-ren-parseint", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "consolidate", "left": {"kind": "inline", "name": "safe_int", "text": "def safe_int(value, default=0):\n    try:\n        return int(value)\n    except (TypeError, ValueError):\n        return default"}, "right": {"kind": "inline", "name": "to_int_or", "text": "def to_int_or(raw, fallback=0):\n    try:\n        return int(raw)\n    except (TypeError, ValueError):\n        return fallback"}, "notes": "Pure rename of a very common helper."}
{"pair_id": "py-neg-sortstability", "split": "train", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "sort_by_name", "text": "def sort_by_name(users):\n    return sorted(users, key=lambda u: u.name)"}, "right": {"kind": "inline", "name": "sort_by_name_ci", "text": "def sort_by_name_ci(users):\n    return sorted(users, key=lambda u: u.name.lower())"}, "notes": "Case-sensitive vs case-insensitive ordering. Differs on mixed-case input."}
{"pair_id": "py-neg-stripchars", "split": "train", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "strip_prefix", "text": "def strip_prefix(name, prefix):\n    if name.startswith(prefix):\n        return name[len(prefix):]\n    return name"}, "right": {"kind": "inline", "name": "lstrip_chars", "text": "def lstrip_chars(name, prefix):\n    return name.lstrip(prefix)"}, "notes": "The classic lstrip misunderstanding: lstrip removes any leading characters in the set, not the prefix string. Differs on 'aabbc'."}
{"pair_id": "py-neg-truthiness", "split": "train", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "first_or_none", "text": "def first_or_none(items):\n    return items[0] if items else None"}, "right": {"kind": "inline", "name": "first_truthy", "text": "def first_truthy(items):\n    for item in items:\n        if item:\n            return item\n    return None"}, "notes": "First element vs first truthy element. Differs whenever items[0] is falsy."}
{"pair_id": "py-neg-copy", "split": "train", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "copy_config", "text": "def copy_config(config):\n    return dict(config)"}, "right": {"kind": "inline", "name": "clone_config", "text": "def clone_config(config):\n    return copy.deepcopy(config)"}, "notes": "Shallow vs deep copy. Identical until a nested value is mutated."}
{"pair_id": "py-neg-timezone", "split": "train", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "day_start", "text": "def day_start(moment):\n    return moment.replace(hour=0, minute=0, second=0, microsecond=0)"}, "right": {"kind": "inline", "name": "day_start_utc", "text": "def day_start_utc(moment):\n    return moment.astimezone(timezone.utc).replace(\n        hour=0, minute=0, second=0, microsecond=0\n    )"}, "notes": "Local vs UTC midnight. Same shape, different instant for non-UTC input."}
{"pair_id": "py-neg-pagination", "split": "train", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "page_slice", "text": "def page_slice(items, page, size):\n    start = page * size\n    return items[start:start + size]"}, "right": {"kind": "inline", "name": "page_slice_one_indexed", "text": "def page_slice_one_indexed(items, page, size):\n    start = (page - 1) * size\n    return items[start:start + size]"}, "notes": "Zero-indexed vs one-indexed pagination. An off-by-one page silently returns the wrong window."}
{"pair_id": "py-neg-json-default", "split": "train", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "load_settings", "text": "def load_settings(path):\n    with open(path) as fh:\n        return json.load(fh)"}, "right": {"kind": "inline", "name": "load_settings_safe", "text": "def load_settings_safe(path):\n    if not os.path.exists(path):\n        return {}\n    with open(path) as fh:\n        return json.load(fh)"}, "notes": "One raises FileNotFoundError, one returns an empty dict."}
{"pair_id": "py-neg-mutable-default", "split": "train", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "append_tag", "text": "def append_tag(tag, tags=[]):\n    tags.append(tag)\n    return tags"}, "right": {"kind": "inline", "name": "append_tag_safe", "text": "def append_tag_safe(tag, tags=None):\n    tags = list(tags or [])\n    tags.append(tag)\n    return tags"}, "notes": "Shared mutable default vs a fresh list. The first accumulates across calls; the second is a real bug fix, not a duplicate."}
{"pair_id": "py-neg-equality", "split": "holdout", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "same_user", "text": "def same_user(a, b):\n    return a.id == b.id"}, "right": {"kind": "inline", "name": "same_user_strict", "text": "def same_user_strict(a, b):\n    return a.id == b.id and a.tenant_id == b.tenant_id"}, "notes": "Cross-tenant identity check. The extra condition is a security boundary."}
{"pair_id": "py-neg-rounding-money", "split": "holdout", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "to_cents", "text": "def to_cents(amount):\n    return int(amount * 100)"}, "right": {"kind": "inline", "name": "to_cents_rounded", "text": "def to_cents_rounded(amount):\n    return int(round(amount * 100))"}, "notes": "Truncation vs rounding on money. Differs by a cent on most float inputs."}
{"pair_id": "py-neg-empty-check", "split": "holdout", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "is_blank", "text": "def is_blank(value):\n    return value is None or value == \"\""}, "right": {"kind": "inline", "name": "is_empty", "text": "def is_empty(value):\n    return not value"}, "notes": "Explicit None/empty-string check vs general falsiness. Differs for 0, [], False."}
{"pair_id": "py-neg-http-verb", "split": "holdout", "language": "python", "label": "not_duplicate", "should_consolidate": "not_applicable", "left": {"kind": "inline", "name": "delete_user", "text": "def delete_user(client, user_id):\n    return client.request(\"DELETE\", f\"/users/{user_id}\")"}, "right": {"kind": "inline", "name": "archive_user", "text": "def archive_user(client, user_id):\n    return client.request(\"POST\", f\"/users/{user_id}/archive\")"}, "notes": "Same wrapper shape, different endpoint and verb. Destructive vs reversible."}
{"pair_id": "py-d16-tenant-boundary", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "leave_alone", "left": {"kind": "inline", "name": "serialize_event", "text": "def serialize_event(event):\n    return {\n        \"id\": str(event.id),\n        \"type\": event.type,\n        \"created_at\": event.created_at.isoformat(),\n    }"}, "right": {"kind": "inline", "name": "serialize_event", "text": "def serialize_event(event):\n    return {\n        \"id\": str(event.id),\n        \"type\": event.type,\n        \"created_at\": event.created_at.isoformat(),\n    }"}, "notes": "Identical, but one lives in the public webhook payload builder and one in the internal analytics exporter. Unifying couples an external contract we cannot change to an internal one we change weekly.", "exclusion": "cross_boundary"}
{"pair_id": "py-d16-migration", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "leave_alone", "left": {"kind": "inline", "name": "upgrade", "text": "def upgrade():\n    op.add_column(\"users\", sa.Column(\"locale\", sa.String(8), nullable=True))\n    op.create_index(\"ix_users_locale\", \"users\", [\"locale\"])"}, "right": {"kind": "inline", "name": "upgrade", "text": "def upgrade():\n    op.add_column(\"teams\", sa.Column(\"locale\", sa.String(8), nullable=True))\n    op.create_index(\"ix_teams_locale\", \"teams\", [\"locale\"])"}, "notes": "Migration files. Historical records that must never be edited or deduplicated.", "exclusion": "generated_or_vendored"}
{"pair_id": "py-d16-tiny-getter", "split": "train", "language": "python", "label": "duplicate", "should_consolidate": "leave_alone", "left": {"kind": "inline", "name": "display_name", "text": "def display_name(user):\n    return user.full_name or user.email"}, "right": {"kind": "inline", "name": "label_for", "text": "def label_for(user):\n    return user.full_name or user.email"}, "notes": "One line. An import costs more than the duplication.", "exclusion": "below_size_floor"}
{"pair_id": "py-d16-vendored", "split": "holdout", "language": "python", "label": "duplicate", "should_consolidate": "leave_alone", "left": {"kind": "inline", "name": "which", "text": "def which(program):\n    for directory in os.environ.get(\"PATH\", \"\").split(os.pathsep):\n        candidate = os.path.join(directory, program)\n        if os.path.isfile(candidate) and os.access(candidate, os.X_OK):\n            return candidate\n    return None"}, "right": {"kind": "inline", "name": "which", "text": "def which(program):\n    for directory in os.environ.get(\"PATH\", \"\").split(os.pathsep):\n        candidate = os.path.join(directory, program)\n        if os.path.isfile(candidate) and os.access(candidate, os.X_OK):\n            return candidate\n    return None"}, "notes": "Both copies sit under vendor/ directories of two bundled third-party packages. Editing vendored code breaks the next upgrade.", "exclusion": "generated_or_vendored"}
{"pair_id": "py-d16-diverging-pricing", "split": "holdout", "language": "python", "label": "duplicate", "should_consolidate": "leave_alone", "left": {"kind": "inline", "name": "compute_discount", "text": "def compute_discount(order):\n    if order.total > 100:\n        return order.total * 0.1\n    return 0"}, "right": {"kind": "inline", "name": "compute_discount", "text": "def compute_discount(order):\n    if order.total > 100:\n        return order.total * 0.1\n    return 0"}, "notes": "Identical today, but the B2B and B2C pricing modules have each been edited three times in the last quarter as their rules separate. Merging now guarantees an un-merge next month.", "exclusion": "diverged_history"}
